# Shared design system package

Package `@dubo/design-system-shared`: tokens, generated theme, primitives and shared UI utilities.

Build with `pnpm build:package` and typecheck with `pnpm typecheck` from the repository root. The
visual language and usage rules live in `DESIGN.md`.
