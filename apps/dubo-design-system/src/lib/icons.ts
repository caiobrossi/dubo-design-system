/**
 * Design system app icon entrypoint.
 * Stories, docs and demos should import icons from here instead of `lucide-react`
 * so the icon library can be swapped centrally later.
 */

export type { IconComponent } from "@dubo-design/lib/icons";
export * from "@dubo-design/lib/icons";
