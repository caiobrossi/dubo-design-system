# Dubo design system app

Documentation, demos and Storybook for the shared design system. Runtime primitives remain owned by
`packages/design-system`; this app consumes them.

Local commands are exposed at the repository root as `pnpm dev`, `pnpm build`, `pnpm storybook` and
`pnpm build:storybook`.
